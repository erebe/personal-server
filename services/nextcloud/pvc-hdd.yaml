apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-hdd-pv-claim
spec:
  storageClassName: zfs-nfs-hdd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
