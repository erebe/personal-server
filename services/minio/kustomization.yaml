apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
labels:
  - includeSelectors: true
    pairs:
      app: minio
resources:
  - deployment.yaml
  - pvc.yaml
generators:
  - secret-generator.yaml
