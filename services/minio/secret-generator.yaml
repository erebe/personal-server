apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: minio-secret
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: /opt/kustomize/ksops
files:
  - ../secrets/minio.yml
