apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
labels:
  - includeSelectors: true
    pairs:
      app: coub
resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - httproute.yaml
generators:
  - secret-generator.yaml
